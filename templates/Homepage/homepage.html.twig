{% extends 'base.html.twig' %}

{% block body %}
    <h1>Top offer</h1>

    {% for product in products %}
        <div>
            <h3>{{ product.name }}</h3>
            <p>
                Price: {{ product.price }}
            </p>
        </div>
    {% endfor %}
{% endblock %}